#pragma once
#include <iostream>

using namespace std;

namespace algoritmy
{
	int& AlgoritmEvklida(int& x, int& y);
	int& BinarniyAlgoritm(int& x, int& y);
	int& RashirenniyAlgoritmEvklida(int& x, int& y);
	int& AlgoritmLemmera(int& x, int& y);
}
